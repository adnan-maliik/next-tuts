import { Spinner } from "reactstrap"

const Loader = () => {
  return (
    <div className="my-5 text-center">
        <Spinner
        color="primary"
        />
    </div>

  )
}

export default Loader